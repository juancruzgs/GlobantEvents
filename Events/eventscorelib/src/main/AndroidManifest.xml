<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.globant.eventscorelib" >

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:label="@string/app_name" >
        <activity
            android:name=".baseComponents.TweetActivity"
            android:label="@string/title_base_activity_twitter"
            android:windowSoftInputMode="adjustResize"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:host="t4jsample"
                    android:scheme="oauth" />
            </intent-filter>
        </activity>
        <activity
            android:name=".baseComponents.SpeakerDetailActivity"
            android:label="@string/title_activity_speaker_detail" >
        </activity>
        <activity
            android:name=".baseComponents.SubscriberActivity"
            android:label="@string/title_activity_subscriber" >
        </activity>
    </application>

</manifest>
